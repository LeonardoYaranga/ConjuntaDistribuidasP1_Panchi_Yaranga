#spring.application.name=examen-yaranga-leonardo

spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/ms_riesgo_crediticio
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
server:
  port: 8080